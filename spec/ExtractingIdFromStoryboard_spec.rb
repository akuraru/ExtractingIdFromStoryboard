require 'spec_helper'

describe ExtractingIdFromStoryboard do
  it 'has a version number' do
    expect(ExtractingIdFromStoryboard::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
